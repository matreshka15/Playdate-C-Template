{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "▶ 运行游戏 (F5)  |  Run Game",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${env:PLAYDATE_SDK_PATH}/bin/PlaydateSimulator.exe",
            "args": [
                "${workspaceFolder}/${config:playdate.projectName}.pdx"
            ],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "internalConsole",
            "preLaunchTask": "Build"
        }
    ]
}
